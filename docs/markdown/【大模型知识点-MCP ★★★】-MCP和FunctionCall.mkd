## [Function Calling]

**[通义千问] ： spring AI 使用 MCP 和 function call 详细代码例子，并介绍两者区别**



在 Spring AI（Spring for Artificial Intelligence）中，**MCP（Model Calling Protocol）** 和 **Function Calling（函数调用）** 是两种与大语言模型（LLM）交互时实现工具调用（Tool Calling）的方式。它们的目标类似——让 LLM 能够调用外部函数或服务，但实现机制和抽象层级不同。

> ⚠️ 注意：截至 2025 年 10 月，Spring AI 仍处于积极开发阶段，MCP 是 Spring AI 团队提出的一种标准化协议，用于统一不同 LLM 的工具调用行为。Function Calling 则是更通用的术语，通常指 LLM 原生支持的函数调用能力（如 OpenAI 的 `function_call`）。



### 一、Function Calling（函数调用）

这是基于 LLM 原生能力（如 OpenAI、Anthropic、Google Vertex AI 等）的机制。开发者定义函数描述（名称、参数、描述），LLM 在生成响应时决定是否调用某个函数，并返回函数调用请求（包括函数名和参数）。开发者需手动解析并执行该函数，再将结果回传给模型。

**✅ 适用场景：**

- 使用原生支持 function calling 的 LLM（如 OpenAI GPT 系列）
- 需要精细控制调用逻辑

**📌 Spring AI 中的 Function Calling 示例（基于 OpenAI）**

```java
// 1. 定义工具函数（使用 Spring AI 的 @Tool 注解）
@Component
public class WeatherService {

    @Tool("获取指定城市的当前天气")
    public String getCurrentWeather(@ToolParam("城市名称") String city) {
        // 模拟调用天气 API
        return "当前 " + city + " 的天气是晴天，气温 25°C。";
    }
}

// 2. 配置 AiClient（使用 OpenAI）
@Configuration
public class AiConfig {

    @Bean
    public AiClient aiClient(ChatClient chatClient) {
        return new AiClient(chatClient);
    }

    @Bean
    public ChatClient chatClient(ChatClientFactory factory) {
        return factory.create("openai"); // 需配置 spring.ai.openai.api-key
    }
}

// 3. 调用示例
@RestController
public class ChatController {

    private final AiClient aiClient;

    public ChatController(AiClient aiClient) {
        this.aiClient = aiClient;
    }

    @GetMapping("/chat")
    public String chat(@RequestParam String message) {
        Prompt prompt = new Prompt(message);
        // Spring AI 自动注册带 @Tool 的方法为可用工具
        AiResponse response = aiClient.generate(prompt);
        return response.getGeneration().getText();
    }
}
```

> Spring AI 会自动扫描 `@Tool` 注解的方法，将其转换为 OpenAI 所需的 `functions` 列表，并在 LLM 请求中传递。当模型决定调用函数时，Spring AI 会自动执行对应方法并拼接结果。



### 二、MCP（Model Calling Protocol）

MCP 是 Spring AI 提出的一种**标准化协议**，旨在**抽象不同 LLM 的工具调用差异**。无论底层是 OpenAI、Ollama、Google PaLM 还是本地模型，只要实现了 MCP 适配器，就可以用统一方式注册和调用工具。

**MCP 的核心思想是：**

- 工具以标准格式注册（名称、输入 schema、描述）
- LLM 通过 MCP 协议“请求调用工具”
- Spring AI 负责调度执行并返回结果

**✅ 适用场景：**

- 多模型切换（如从 OpenAI 切换到本地 Llama 3）
- 希望代码与具体 LLM 解耦
- 使用不原生支持 function calling 的模型（通过 MCP 模拟）

**📌 Spring AI 中的 MCP 示例**

```java
// 1. 定义 MCP 工具（实现 Tool 接口或使用 @McpTool）
@McpTool(description = "获取用户所在城市的天气信息")
public class WeatherMcpTool implements Tool {

    @Override
    public String getName() {
        return "get_weather";
    }

    @Override
    public JsonNode execute(JsonNode input) {
        String city = input.get("city").asText();
        String result = "当前 " + city + " 天气晴朗，25°C。";
        ObjectNode output = JsonNodeFactory.instance.objectNode();
        output.put("weather", result);
        return output;
    }

    @Override
    public JsonSchema getInputSchema() {
        return JsonSchema.builder()
            .type("object")
            .property("city", JsonSchema.builder().type("string").description("城市名称").build())
            .required("city")
            .build();
    }
}

// 2. 注册 MCP 工具到 AiClient
@Configuration
public class McpConfig {

    @Bean
    public AiClient aiClient(ChatClient chatClient, List<Tool> tools) {
        return AiClient.builder()
            .chatClient(chatClient)
            .tools(tools) // 自动注入所有 Tool 实现（包括 @McpTool）
            .build();
    }
}

// 3. 控制器调用（与 Function Calling 相同）
@RestController
public class McpController {

    private final AiClient aiClient;

    public McpController(AiClient aiClient) {
        this.aiClient = aiClient;
    }

    @GetMapping("/mcp-chat")
    public String chat(@RequestParam String message) {
        Prompt prompt = new Prompt(message);
        AiResponse response = aiClient.generate(prompt);
        return response.getGeneration().getText();
    }
}
```

> 在 MCP 模式下，即使底层模型（如 Ollama 上的 Llama 3）不支持原生 function calling，Spring AI 也可以通过提示词工程（prompt engineering）模拟工具调用流程。



### 三、MCP 与 Function Calling 的核心区别

| 特性                   | Function Calling                       | MCP                          |
| - | -- | - |
| **依赖 LLM 原生能力**  | 是（如 OpenAI）                        | 否（可适配任意模型）         |
| **标准化程度**         | 依赖厂商 API（OpenAI/Google 各自不同） | Spring AI 统一协议           |
| **代码耦合度**         | 与特定 LLM 绑定较强                    | 与模型解耦，便于切换         |
| **适用模型范围**       | 仅支持 function calling 的模型         | 所有模型（包括本地开源模型） |
| **实现复杂度**         | 简单（自动集成）                       | 稍复杂（需定义 schema）      |
| **Spring AI 抽象层级** | 较低（直接映射到 LLM API）             | 较高（统一中间层）           |



### 四、如何选择？

- 如果你**只用 OpenAI 或 Anthropic**，且追求简单，用 **Function Calling（`@Tool`）**。
- 如果你计划**支持多种模型**（包括本地模型如 Llama、Phi、Qwen），或希望**未来可迁移**，推荐使用 **MCP（`@McpTool` 或 `Tool` 接口）**。



### 五、补充说明

- Spring AI 的 `@Tool` 注解底层在 OpenAI 场景下会自动转为 function calling；在非原生支持的模型上，会 fallback 到 MCP 风格的提示词模拟。
- MCP 是 Spring AI 的前瞻性设计，未来可能成为主流工具调用标准。



如需完整可运行项目，可参考官方示例：
- https://github.com/spring-projects/spring-ai/tree/main/spring-ai-examples

如有具体模型（如 Ollama + Llama 3）或部署场景，也可提供更针对性的代码。





## [FunctionCall工具调用机制] ★★★

代码来自

https://github.com/apollyon0728/my-ai-agent.git

### WeatherSearchTool

```java
package com.yam.myaiagent.tools;

import cn.hutool.http.HttpRequest;
import cn.hutool.http.HttpResponse;
import cn.hutool.json.JSONObject;
import cn.hutool.json.JSONUtil;
import org.springframework.ai.tool.annotation.Tool;
import org.springframework.ai.tool.annotation.ToolParam;

import java.util.List;
import java.util.Map;

/**
 * 天气查询工具类
 */
public class WeatherSearchTool {

    @Tool(description = "Search for weather information by city name")
    public String searchWeather(
            @ToolParam(description = "the name of search city") String city) {

        // 构建请求URL
        String url = String.format("https://api.pearktrue.cn/api/weather/?city=%s&id=1", city);

        try {
            // 发送GET请求
            HttpResponse response = HttpRequest.get(url).execute();

            if (response.isOk()) {
                String body = response.body();
                JSONObject json = JSONUtil.parseObj(body);

                // 解析响应数据
                if (json.getInt("code") == 200) {
                    String cityName = json.getStr("city");
                    List<Map<String, Object>> data = json.get("data", List.class);

                    StringBuilder result = new StringBuilder();
                    result.append("城市: ").append(cityName).append("\n");
                    result.append("天气预报:\n");

                    for (Map<String, Object> day : data) {
                        result.append(day.get("date")).append(": ")
                                .append(day.get("weather")).append(", ")
                                .append("温度: ").append(day.get("temperature")).append(", ")
                                .append(day.get("wind")).append(" ").append(day.get("wind_level")).append(", ")
                                .append("空气质量: ").append(day.get("air_quality")).append("\n");
                    }

                    return result.toString();
                } else {
                    return "查询失败: " + json.getStr("msg");
                }
            } else {
                return "请求天气API失败，状态码: " + response.getStatus();
            }
        } catch (Exception e) {
            return "查询天气时发生错误: " + e.getMessage();
        }
    }
}
```



### doChatWithTools

.tools(allTools)

```java
/**
 * Java面试功能（支持调用工具）
 * 可以调用各种工具来辅助面试，如生成代码、查询资料等
 *
 * @param message 用户消息
 * @param chatId 会话ID
 * @return 回复内容
 */
public String doChatWithTools(String message, String chatId) {
    ChatResponse chatResponse = chatClient
            .prompt()
            .system(SYSTEM_PROMPT + """
                    
                    你现在可以使用各种工具来辅助Java面试过程：
                    • 可以生成代码示例和技术文档
                    • 可以搜索最新的Java技术资料
                    • 可以创建面试题目和答案文档
                    • 可以进行在线编程练习
                    
                    请根据需要合理使用工具，提供更好的面试辅导体验。
                    """)
            .user(message)
            .advisors(spec -> spec.param(CHAT_MEMORY_CONVERSATION_ID_KEY, chatId)
                    .param(CHAT_MEMORY_RETRIEVE_SIZE_KEY, 10))
            .advisors(new MyLoggerAdvisor())
            .tools(allTools)
            .call()
            .chatResponse();
    String content = chatResponse.getResult().getOutput().getText();
    log.info("Java面试工具调用结果: {}", content);
    return content;
}
```



### 什么来决策发起对tool工具的调用?

**[JD-AI] ：上面代码中实现了 function call tool的实现和应用，目前疑问是由什么来决策发起对tool工具的调用，并且大模型如何决定使用某种工具？**

**Spring AI中的Function Call工具调用机制**

在Spring AI中，Function Call（工具调用）的决策和执行主要由以下几个部分共同完成：

#### 1. 工具定义与注册

从您提供的代码可以看到，工具是通过`@Tool`注解定义的，例如：

```java
@Tool(description = "分析Java代码质量，检查常见问题和性能优化建议")
public String analyzeJavaCode(@ToolParam(description = "要分析的Java代码") String javaCode) {
    // 实现代码
}
```

这些工具会被注册到Spring容器中，并通过`ToolCallback`接口进行管理。在`JavaInterviewApp`中，工具通过依赖注入的方式被引入：

```java
@Resource
private ToolCallback[] allTools;
```

#### 2. 工具调用的决策机制

**大模型决策调用工具的关键在于系统提示词和上下文**：

1. **系统提示词中的工具介绍**：

   ```java
   .system(SYSTEM_PROMPT + """
           
           你现在可以使用各种工具来辅助Java面试过程：
           • 可以生成代码示例和技术文档
           • 可以搜索最新的Java技术资料
           • 可以创建面试题目和答案文档
           • 可以进行在线编程练习
           
           请根据需要合理使用工具，提供更好的面试辅导体验。
           """)
   ```

   这段提示词告诉大模型它可以使用哪些工具以及何时使用它们。

2. **工具的注册与描述**： 当调用`chatClient.prompt().tools(allTools)`时，Spring AI会将工具的名称、描述和参数信息发送给大模型。大模型根据这些描述了解每个工具的功能和使用方法。

3. **大模型的决策过程**：

   - 大模型分析用户的问题和需求
   - 根据工具的描述，判断哪个工具最适合解决当前问题
   - 决定是直接回答还是调用工具获取更多信息
   - 如果决定调用工具，会生成符合工具参数要求的调用请求

#### 3. 工具调用的执行流程

1. 用户发送消息到`doChatWithTools`方法
2. 消息和系统提示词被发送给大模型
3. 大模型分析内容，决定是否需要调用工具
4. 如果需要调用工具，大模型会生成工具调用请求（包含工具名和参数）
5. Spring AI框架接收这个请求，找到对应的工具实现并执行
6. 工具执行结果返回给大模型
7. 大模型结合工具执行结果生成最终回复

#### 4. 工具选择的决定因素

大模型选择使用哪个工具主要基于：

- **用户问题的内容和意图**：例如，当用户询问天气时，会选择`WeatherSearchTool`
- **工具的描述和功能**：通过`@Tool`注解中的description了解工具用途
- **参数的匹配度**：用户提供的信息是否足够调用特定工具
- **上下文相关性**：当前对话的主题和历史

总结来说，Spring AI中的工具调用是由大模型根据用户需求、工具描述和上下文来决策的，而Spring框架负责工具的注册、调用和结果处理。大模型通过系统提示词了解可用工具，并在适当的时候选择调用它们。



## 【Spring AI中的工具匹配机制及跨语言调用原理】

**大模型是如何找到对应的工具?**

**[JD-AI] ：大模型是如何找到对应的工具的，这块还不太明白？ **

比如

```java
@Tool(description = "Search for weather information by city name")
public String searchWeather(
        @ToolParam(description = "the name of search city") String city) { 
```



这块是通过什么方式找到要调用这个工具的，通过Tool(description 吗？ 那么问题是我**`如果问话时用的中文比如说问今天天气，但是这个tool的description的语言是英文`，也能找到这个工具吗？ 什么原理? **



关于您的问题，大模型是如何找到对应工具以及跨语言调用的原理，我来详细解释：

#### 工具匹配机制

Spring AI中的工具匹配主要通过以下流程实现：

1. **工具注册与元数据提取**：
   - **当您使用`@Tool`注解标记方法时，Spring AI会提取该方法的元数据，包括：**
     - 方法名称
     - 描述信息（通过`description`属性提供）
     - 参数列表及其描述（通过`@ToolParam`注解提供）
     - 返回类型信息
2. **工具信息传递给大模型**：
   - 当执行`chatClient.prompt().tools(allTools).call()`时，Spring AI会将所有工具的元数据转换为标准格式（通常是JSON格式）
   - 这些元数据会作为请求的一部分发送给大模型API
3. **大模型的语义理解与匹配**：
   - 大模型会分析用户的输入内容（问题或指令）
   - 同时分析所有可用工具的描述和功能
   - 通过**`语义理解`**（而非简单的关键词匹配）来确定最合适的工具

#### 跨语言调用原理

关于您提到的中文问题匹配英文工具描述的情况：

1. **`语义理解而非关键词匹配`**：
   - 大模型具有跨语言理解能力，它不仅仅是寻找关键词匹配
   - 它理解"今天天气"和"weather information"在语义上是相关的概念
2. **上下文与意图识别**：
   - 大模型会分析用户问题的意图（查询天气）
   - 然后寻找能满足这一意图的工具，无论其描述使用何种语言
3. **工具描述的作用**：
   - `@Tool(description = "Search for weather information by city name")`提供了工具的功能描述
   - 大模型通过理解这个描述的语义，而非仅仅匹配关键词，来确定工具的用途
4. **参数映射**：
   - 当确定使用某个工具后，大模型会尝试从用户输入中提取必要的参数
   - 例如，从"今天北京天气怎么样"中提取出"北京"作为city参数

#### 工作原理示例

当用户用中文问"今天北京天气怎么样"时：

1. 大模型分析问题，理解用户想查询天气信息
2. 检查所有可用工具，发现`searchWeather`工具的描述是关于天气查询的
3. 尽管描述是英文的，但大模型理解其语义与用户意图匹配
4. 从用户问题中提取"北京"作为city参数
5. 调用`searchWeather("北京")`方法
6. 获取结果并生成最终回复

#### 为什么能跨语言工作？

这种跨语言能力主要归功于：

1. **大模型的多语言训练**：大模型在训练时接触了大量多语言数据，能够理解不同语言表达的相同概念
2. **语义理解而非字面匹配**：大模型关注的是概念和意图，而非简单的字符串匹配
3. **上下文感知能力**：大模型会考虑整个对话上下文，而不仅仅是单个问题

因此，即使工具描述是英文，用户提问是中文，大模型仍能正确识别并调用适当的工具。这也是为什么Spring AI的工具调用机制能够在多语言环境中有效工作的原因。























